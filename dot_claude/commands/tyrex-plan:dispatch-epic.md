---
allowed-tools: Read, Glob, Grep, Write, Bash, WebFetch
description: Create GitLab issues and Obsidian structure from an epic plan
---

Load the `tyrex-plans` skill and execute **Mode 2: Dispatch Epic** with argument `$ARGUMENTS`.

The argument is the path to the epic plan file (e.g., `Plans/epic-119-usb-storage.md`).
