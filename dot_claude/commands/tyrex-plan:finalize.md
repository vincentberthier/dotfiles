---
allowed-tools: Read, Glob, Grep, Write, Edit, Bash, WebFetch
description: Run final audit and create MR for a completed issue or epic
---

Load the `tyrex-plans` skill and execute **Mode 4: Finalize** with argument `$ARGUMENTS`.

The argument is `#NNN` for a standalone issue or `&NNN` for an epic.
