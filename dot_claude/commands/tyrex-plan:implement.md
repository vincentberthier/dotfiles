---
allowed-tools: Read, Glob, Grep, Write, Edit, Bash, WebFetch
description: Load an issue plan from Obsidian and implement it with progress tracking
---

Load the `tyrex-plans` skill and execute **Mode 3: Implement** with argument `$ARGUMENTS`.

The argument is the issue number (e.g., `32`).
