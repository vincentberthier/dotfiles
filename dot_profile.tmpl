# Environment variables
export EDITOR="nvim"
export TERMINAL="terminator"
export BROWSER="firefox"

# XDG config paths
export XDG_CONFIG_HOME=$HOME/.config
export XDG_CACHE_HOME=$HOME/.cache
export XDG_DATA_HOME=$HOME/.local/share

# ZSH configuration path
export ZDOTDIR=$HOME/.config/zsh

# Add user binaries to path

# Launch ssh agent server
SSH_ENV="$HOME/.ssh/agent-environment"

function start_agent {
    echo "Initialising new SSH agent..."
    /usr/bin/ssh-agent | sed 's/^echo/#echo/' > "${SSH_ENV}"
    echo succeeded
    chmod 600 "${SSH_ENV}"
    . "${SSH_ENV}" > /dev/null
    /usr/bin/ssh-add > /dev/null
}

# Source SSH settings, if applicable
if [ -f "${SSH_ENV}" ]; then
    . "${SSH_ENV}" > /dev/null
    #ps ${SSH_AGENT_PID} doesn't work under cywgin
    ps -ef | grep ${SSH_AGENT_PID} | grep ssh-agent$ > /dev/null || {
        start_agent;
    }
else
    start_agent;
fi  

{{ if (eq .chezmoi.hostname "px-1021")}}host=`hostname`
if [[ $host == "px-1021" ]]; then
    alias zsh='/homelocal/vberthier/.conda/envs/default/bin/zsh'
    export HTTP_PROXY=http://proxy-bureautique.cls.fr:8080/
    export http_proxy=http://proxy-bureautique.cls.fr:8080/
    export HTTPS_PROXY=http://proxy-bureautique.cls.fr:8080/
    export https_proxy=http://proxy-bureautique.cls.fr:8080/
else
    export PATH=$HOME/bin:$HOME/.local/bin:$PATH
    export LD_LIBRARY_PATH=$HOME/.local/lib:$LD_LIBRARY_PATH
fi{{ else }}export PATH=$HOME/bin:$HOME/.local/bin:$PATH{{ end }}

{{ if (eq .chezmoi.hostname "visu01") }}export FPATH=$HOME/.local/share/zsh/5.9/functions
export LD_LIBRARY_PATH=$HOME/.local/lib:$LD_LIBRARY_PATH{{ end }}

if [ -f "$HOME/.cargo/env" ]; then . $HOME/.cargo/env; fi
 {{ if (eq .chezmoi.hostname "px-1021") }}[[ $- == *i* ]] && zsh || return{{ end }}
