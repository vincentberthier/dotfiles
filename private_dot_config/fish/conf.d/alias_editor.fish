### Editors
#
# shx (helix as root, with the user config) lives in functions/.

alias hx="helix"
alias editf="hx (fzf)"

# Don’t use vim but nvim
alias svi="doas nvim"
alias vdev='nvim --cmd "set rtp+=./" .'
